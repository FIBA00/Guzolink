# Production: empty = same-origin (backend serves frontend from /dist)
# If frontend is on a separate domain, set: VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=http://localhost:8000
VITE_BASE_URL=/guzolink/

