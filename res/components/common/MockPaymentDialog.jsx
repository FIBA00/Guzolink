/** Style: Market Ledger — a visible sandbox outcome switch lets teams exercise both provider paths without collecting payment details. */
import { AlertTriangle, CheckCircle2, CreditCard, LoaderCircle, RotateCcw, ShieldCheck } from "lucide-react";
import { useState } from "react";
import Modal from "../ui/Modal";
import { processMockPayment } from "../../services/mockPaymentProvider";
import { formatCurrency } from "../../lib/utils";

export default function MockPaymentDialog({ open, onClose, amount, orderReference, onSuccess, onFailure }) { const [state, setState] = useState("ready"); const [outcome, setOutcome] = useState("success"); async function startPayment() { setState("processing"); try { const result = await processMockPayment({ amount, orderReference, outcome }); setState("complete"); globalThis.setTimeout(() => { onSuccess(result); setState("ready"); }, 420); } catch (error) { setState("failed"); onFailure?.(error); } } function close() { if (state !== "processing") { setState("ready"); onClose(); } } const failed = state === "failed"; const completed = state === "complete"; return <Modal open={open} onClose={close} title={completed ? "Payment approved." : failed ? "Payment failed." : "Confirm payment handoff"} eyebrow="LedgerPay Sandbox" size="max-w-lg">{completed ? <div className="py-4 text-center"><CheckCircle2 className="mx-auto text-moss" size={44} /><p className="mt-4 text-sm leading-6 text-[#626861]">The simulated provider approved this payment. Returning to your order confirmation now.</p></div> : failed ? <div className="py-2"><div className="flex gap-3 border-b border-line pb-5"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-[#f6ddd4] text-clay"><AlertTriangle size={19} /></span><div><p className="text-sm font-extrabold">The sandbox provider declined this test transaction.</p><p className="mt-1 text-sm leading-6 text-[#666c65]">No order was placed and no payment data was collected. Switch to a successful simulation and retry when ready.</p></div></div><div className="mt-6 flex flex-wrap justify-end gap-3"><button className="button-secondary" onClick={close}>Return to checkout</button><button className="button-primary" onClick={() => { setOutcome("success"); setState("ready"); }}><RotateCcw size={16} /> Retry as successful</button></div></div> : <><div className="flex gap-3 border-b border-line pb-5"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-[#f2e1bf] text-ochre-dark"><CreditCard size={19} /></span><div><p className="text-sm font-extrabold">You are leaving Guzolink briefly.</p><p className="mt-1 text-sm leading-6 text-[#666c65]">This sandbox simulates a provider-neutral payment handoff. No card or payment information is collected.</p></div></div><div className="mt-5 flex items-center justify-between border border-line bg-[#faf5ea] p-4"><span className="text-sm font-extrabold">Order amount</span><span className="text-xl font-extrabold text-ochre-dark">{formatCurrency(amount)}</span></div><fieldset className="mt-5"><legend className="field-label">Sandbox transaction outcome</legend><div className="grid gap-2 sm:grid-cols-2"><button type="button" aria-pressed={outcome === "success"} onClick={() => setOutcome("success")} className={`border p-3 text-left text-sm font-extrabold ${outcome === "success" ? "border-moss bg-[#e9f0e4] text-moss" : "border-line bg-[#fffdf7]"}`}><CheckCircle2 className="mb-2" size={17} /> Simulate success</button><button type="button" aria-pressed={outcome === "failure"} onClick={() => setOutcome("failure")} className={`border p-3 text-left text-sm font-extrabold ${outcome === "failure" ? "border-clay bg-[#f8e8e2] text-clay" : "border-line bg-[#fffdf7]"}`}><AlertTriangle className="mb-2" size={17} /> Simulate failure</button></div></fieldset><p className="mt-5 flex items-center gap-2 text-xs leading-5 text-[#6d726c]"><ShieldCheck size={15} className="shrink-0 text-moss" /> In a live setup, this point forwards the order reference to the merchant’s selected payment provider.</p><div className="mt-7 flex flex-wrap justify-end gap-3"><button className="button-secondary" onClick={close} disabled={state === "processing"}>Cancel</button><button className="button-primary" onClick={startPayment} disabled={state === "processing"}>{state === "processing" ? <><LoaderCircle className="animate-spin" size={16} /> Verifying with provider…</> : <><CreditCard size={16} /> {outcome === "failure" ? "Simulate failed payment" : `Pay ${formatCurrency(amount)} in sandbox`}</>}</button></div></>}</Modal>; }
