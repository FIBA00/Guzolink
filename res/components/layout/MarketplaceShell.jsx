/** Style: Market Ledger — public navigation uses a deliberate, accessible paper sidebar rather than a fragile inline menu. */
import { useEffect, useState } from "react";
import { Link, NavLink } from "react-router-dom";
import { Menu, Search, ShoppingBag, Store, X } from "lucide-react";
import ActivityCenter from "../common/ActivityCenter";
import BrandMark from "../common/BrandMark";
import LanguageSelector from "../common/LanguageSelector";
import { useTranslation } from "../../i18n/useTranslation";
import { useAuthStore } from "../../store/authStore";
import { useCartStore } from "../../store/cartStore";

export default function MarketplaceShell({ children }) {
  const [menuOpen, setMenuOpen] = useState(false); const itemCount = useCartStore((state) => state.items.reduce((total, item) => total + item.quantity, 0)); const user = useAuthStore((state) => state.user); const { t } = useTranslation();
  const navItems = [{ to: "/marketplace", label: t("nav.browse") }, { to: "/shops", label: t("nav.shops") }, { to: "/dashboard", label: t("nav.sell") }]; const linkClass = ({ isActive }) => `text-sm font-bold transition-colors ${isActive ? "text-ochre-dark" : "text-[#585e58] hover:text-ink"}`;
  useEffect(() => { if (!menuOpen) return undefined; const closeOnEscape = (event) => { if (event.key === "Escape") setMenuOpen(false); }; window.addEventListener("keydown", closeOnEscape); const previous = document.body.style.overflow; document.body.style.overflow = "hidden"; return () => { window.removeEventListener("keydown", closeOnEscape); document.body.style.overflow = previous; }; }, [menuOpen]);
  const accountTo = user?.role === "admin" ? "/admin" : user?.role === "merchant" ? "/dashboard" : "/account"; const accountLabel = user ? user.role === "admin" ? "Admin" : user.role === "merchant" ? t("nav.workspace") : t("nav.account") : t("nav.signIn");
  return <div className="min-h-screen bg-paper paper-noise text-ink"><header className="sticky top-0 z-40 border-b border-line bg-[#fbf7ee]/95 backdrop-blur"><div className="mx-auto flex h-[4.65rem] max-w-[1440px] items-center justify-between px-4 md:px-8"><BrandMark /><nav className="hidden items-center gap-7 lg:flex">{navItems.map((item) => <NavLink key={item.to} to={item.to} className={linkClass}>{item.label}</NavLink>)}</nav><div className="flex items-center gap-2"><Link aria-label={t("nav.search")} to="/marketplace" className="icon-button hidden sm:grid"><Search size={18} /></Link><LanguageSelector /><ActivityCenter /><Link aria-label={`${t("nav.cart")}, ${itemCount} items`} to="/cart" className="icon-button relative"><ShoppingBag size={18} />{itemCount > 0 && <span className="absolute -right-1 -top-1 grid h-4 min-w-4 place-items-center rounded-full bg-ochre px-1 text-[9px] font-extrabold text-white">{itemCount}</span>}</Link><Link className="button-secondary hidden md:inline-flex" to={user ? accountTo : "/login"}>{accountLabel}</Link><button type="button" className="icon-button" aria-label={menuOpen ? t("nav.closeMenu") : t("nav.menu")} aria-controls="marketplace-sidebar" aria-expanded={menuOpen} onClick={() => setMenuOpen(true)}><Menu size={20} /></button></div></div></header><div className={`fixed inset-0 z-[60] ${menuOpen ? "pointer-events-auto" : "pointer-events-none"}`} aria-hidden={!menuOpen}><button type="button" aria-label={t("nav.closeMenu")} className={`absolute inset-0 bg-ink/55 transition-opacity duration-200 ${menuOpen ? "opacity-100" : "opacity-0"}`} onClick={() => setMenuOpen(false)} /><aside id="marketplace-sidebar" role="dialog" aria-modal="true" aria-label={t("nav.menu")} className={`absolute right-0 top-0 flex h-full w-[min(23rem,88vw)] flex-col bg-[#fffdf7] shadow-2xl transition-transform duration-200 ${menuOpen ? "translate-x-0" : "translate-x-full"}`}><header className="flex items-center justify-between border-b border-line p-5"><BrandMark compact /><button type="button" className="icon-button" aria-label={t("nav.closeMenu")} onClick={() => setMenuOpen(false)}><X size={19} /></button></header><nav className="grid p-5">{navItems.map((item) => <NavLink key={item.to} to={item.to} onClick={() => setMenuOpen(false)} className={({ isActive }) => `border-b border-line py-4 text-lg font-extrabold ${isActive ? "text-ochre-dark" : "text-ink"}`}>{item.label}</NavLink>)}<NavLink to="/help" onClick={() => setMenuOpen(false)} className="border-b border-line py-4 text-lg font-extrabold">Help centre</NavLink><NavLink to="/merchant/onboarding" onClick={() => setMenuOpen(false)} className="border-b border-line py-4 text-lg font-extrabold">Merchant guide</NavLink><NavLink to={user ? accountTo : "/login"} onClick={() => setMenuOpen(false)} className="border-b border-line py-4 text-lg font-extrabold">{accountLabel}</NavLink></nav><div className="mt-auto border-t border-line bg-[#f4ecdc] p-5"><p className="ledger-label">{t("label.systemActivity")}</p><p className="mt-3 text-sm leading-6 text-[#656b64]">{t("home.title")}</p><div className="mt-5"><LanguageSelector /></div></div></aside></div><main>{children}</main><footer className="mt-16 border-t border-line bg-[#1c211f] px-4 py-12 text-[#f7f1e5] md:px-8"><div className="mx-auto grid max-w-[1440px] gap-8 md:grid-cols-[1.3fr_1fr_1fr]"><div><BrandMark dark /><p className="mt-4 max-w-xs text-sm leading-6 text-[#c9c6bc]">A more useful place for local shops to be found, trusted, and bought from.</p><div className="mt-5 flex flex-wrap gap-x-4 gap-y-2 text-xs text-[#d6d0c2]"><Link to="/help">Help centre</Link><Link to="/policies/terms">Terms</Link><Link to="/policies/privacy">Privacy</Link><Link to="/policies/returns">Returns</Link></div></div><div><p className="ledger-label !text-[#d8d0be]">For customers</p><div className="mt-4 grid gap-3 text-sm text-[#e9e4d7]"><Link to="/marketplace">Browse goods</Link><Link to="/shops">Meet the shops</Link><Link to="/orders">My orders</Link><Link to="/growth">Privacy and preferences</Link></div></div><div><p className="ledger-label !text-[#d8d0be]">For merchants</p><div className="mt-4 grid gap-3 text-sm text-[#e9e4d7]"><Link to="/dashboard">Open workspace</Link><Link to="/dashboard/products">Manage products</Link><Link to="/merchant/onboarding">Merchant guide</Link><Link to="/policies/merchant">Merchant agreement</Link></div></div></div></footer><nav className="fixed inset-x-0 bottom-0 z-40 flex h-[4.55rem] items-center justify-around border-t border-line bg-[#fffdf7] lg:hidden"><Link className="grid place-items-center gap-1 text-[10px] font-extrabold" to="/marketplace"><Search size={18} />{t("nav.browse")}</Link><Link className="grid place-items-center gap-1 text-[10px] font-extrabold" to="/shops"><Store size={18} />{t("nav.shops")}</Link><Link className="grid place-items-center gap-1 text-[10px] font-extrabold" to="/cart"><ShoppingBag size={18} />{t("nav.cart")}</Link></nav></div>;
}
