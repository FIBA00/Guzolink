/** Style: Market Ledger — shop cards make the maker, locality, and browsing route visible at a glance. */
import { ArrowUpRight, MapPin } from "lucide-react";
import { Link } from "react-router-dom";
export default function ShopCard({ shop }) { return <Link to={`/shops/${shop.slug}`} className="card-hover group registration-mark block overflow-hidden border border-line bg-[#fffdf7]"><div className="relative aspect-[1.45] overflow-hidden"><img src={shop.image} alt="" loading="lazy" className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.035]" /><div className="absolute inset-0 bg-gradient-to-t from-ink/70 via-ink/5 to-transparent" /><p className="absolute bottom-3 left-4 text-[.68rem] font-extrabold uppercase tracking-[.12em] text-white">{shop.category}</p></div><div className="flex items-start justify-between gap-4 p-4"><div><h3 className="text-base font-extrabold tracking-[-.025em]">{shop.name}</h3><p className="mt-1 flex items-center gap-1 text-xs text-[#646963]"><MapPin size={13} />{shop.location} · {shop.products} products</p><p className="mt-3 text-xs leading-5 text-[#626861]">{shop.description}</p></div><ArrowUpRight className="mt-1 text-ochre-dark" size={20} /></div></Link>; }
